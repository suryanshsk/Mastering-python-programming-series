class Dog:
    def __init__(self, name, breed):
        self.name = name
        self.breed = breed

    def bark(self):
        return f"{self.name} says Woof!"
#class, Object
#dog: Name of a class
#self: Object
#name,bredd: Parmeter
"""self.name = name: Stores the given name inside the object, 
           so you can access it later as object.name"""

"""Class: A blueprint for creating objects.
Object: An instance of a class.
Attributes: Characteristics of the object (variables).
Methods: Actions the object can perform (functions inside the class).
"""
