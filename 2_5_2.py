# Indentation in Python
if True:
    print("This is inside the if block")
    if True:
        print("This is inside the nested if block")
    print("This is still inside the outer if block")
print("This is outside the if block")
